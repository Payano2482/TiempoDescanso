import { TiempoFormatoPipe } from './tiempo-formato.pipe';

describe('TiempoFormatoPipe', () => {
  it('create an instance', () => {
    const pipe = new TiempoFormatoPipe();
    expect(pipe).toBeTruthy();
  });
});
